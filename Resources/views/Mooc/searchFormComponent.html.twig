{# Display search form #}
<li role="presentation">
    <form class="slrn-search-form slrn-search-form--inactive" method="POST" action="{{ path('orange_search') }}">
        <input name="keywords" placeholder="{{ 'search'|trans({}, 'platform') }}" class="slrn-search-form__input">
    </form>
</li>